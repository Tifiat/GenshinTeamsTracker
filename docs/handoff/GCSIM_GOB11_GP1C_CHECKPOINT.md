# GP-1c checkpoint — 2026-09-13

Historical accepted checkpoint, superseded by installed GP-3 on 2026-09-16.
GP-1c-C is the pinned rollback; the shared-EM/scalar response gate is closed.
Current evidence and resume: `GCSIM_GOB11_GP3_CHECKPOINT.md`. Engine IDs,
not-run statements and next-step labels below describe the 2026-09-13 stage,
not the current installation or open task list.

GP-1c-A/B/C cover the bounded original-flat Stat/helper/additive paths tested
here, not general formula completeness or cloud support. Current receipt:
`gob11_additive_receipt_v1.json`; A/B receipts are historical checkpoints.
No live account, equipment, source
note, saved Chasca rotation or UI changes; no full Selected/n1000 runs.

## Implemented boundary

- Original queued FlatDmg expressions may consume an official-import-qualified
  Stat enum. Its runtime String() key and owner become artifact coordinates;
  no character/reaction benefit registry or copied coefficient table is added.
- Direct getter calls and proven single-definition getter aliases are captured
  once. Distinct calls remain distinct even with identical spelling.
- A compound local such as `flat = Stat(EM)*k` is not the stat itself. It stays
  frozen until original evaluation provenance exists; applying k twice is forbidden.
  Preserve the raw use-site AST before alias resolution. Synthetic tests cover
  direct/local/duplicate/compound/alias/import cases and getter call counts.
- Structural template completeness and external parameter ancestry are separate.
  All template inputs are bound. Typed reads with frozen external modifiers
  remain marked incomplete; compact output diagnoses them. The retained Python
  forensic reader now accepts this explicit partial stat and reports uncertainty,
  while still rejecting missing ownership/stat coordinates and malformed templates.
- B follows a unique zero-argument float64 method on the same typed receiver,
  used directly as original FlatDmg. Discovery is structural, not by character
  or helper gameplay names. Budget:128 AST nodes,1..8 returns, no loops/closures/
  defer/goroutines. The existing numeric emitter handles the executed return;
  unsupported operands remain frozen locally. No general argument/local tracing
  or recursive nested-helper unfolding is claimed by this pilot.
- The original call executes once at its original field position, preserving
  earlier field side effects. Only metadata storage is declared before the
  statement. Runtime capture is invocation-local, depth-bounded at5 and panic
  safe; missing/multiple/mismatched records cannot reuse a previous return.
  Trace-disabled calls create no producer metadata. The original guard is NOT
  reevaluated under candidate stats: this is an explicit frozen topology boundary.
- The returned numeric reference reaches the actual hit's support ancestry.
  Ownership comes from its stat reads, not the final trigger. Unknown later
  FlatDmg additions are not silently discarded: unresolved terminal differences
  freeze that flat operand with a diagnostic.
- C retains the full live numeric-write ledger, including writes outside an
  event callback. Original producer, additive changes and later replacements/
  multiplications compose in order. Callback subsets are not counted twice.
- A nested source recipe may supply a numeric-write operand. It uses the same
  arithmetic compiler, WITHOUT an extra crit. Typed live-stat references retain
  their actual owner and available modifier ancestry. Limits:depth5,512 total
  nodes/inputs; malformed/unknown/cyclic portions remain diagnosed local freezes.
- The core/reactable calculation seams carry observation markers. Coefficients
  and operation order are extracted from the engine source; changing those
  coefficients is covered by source tests. No consumer reaction-name switch or
  copied mathematical formula was added. Reaction bonus remains explicitly
  frozen; level-base is fixed because artifact candidates do not change level.
- A terminal source binding includes later additions. Validate the original
  template against the value after rewinding observed writes, NOT against the
  binding's terminal value. Ordered recomposition then validates the final field.

## Measured result

For final C: five saved baseline n1 captures, four changed-stat n1 captures,
seed742031889. One preliminary9-capture batch exposed the original-vs-terminal
comparison defect; it was fixed and the batch repeated. Two additional n1
captures check the installed engine. Updater gates have their own short checks.
Baseline damage agrees within floating precision (max7.28e-12 absolute per hit);
both Chasca channel arrays are identical. Reaction/cross-owner checks still pass.
Installed production also matches the isolated engine per hit at baseline and
three offline stat changes, on baseline and changed-Lauma captures. Raw node
lists are not byte-identical across captures; do not equate allocation/order
differences with damage changes. Validation used the actual Go evaluator, not
a second mathematical implementation. Node-list canonicalization is not part
of this bounded repair.

- C adds EM response on66 hits: Lauma4 normal/30 skill, Kuki18 skill/14 burst.
  Counts describe this trace, not gameplay rules or universal hit counts.
- Every543-hit bloom capture matches per hit at three changed-stat points:
  +187 Lauma EM, +187 Kuki EM, +20% Columbina HP. Relative tolerance1e-10.
  Lauma/Kuki personal EM residuals measured after B are now closed. All groups,
  not only reaction groups or the team's final sum, were checked.
- Four portable fresh-engine slices (99/108/101/101 nodes) preserve original
  producer plus additive response. Go sourcegen/compact/info/optimization/combat/
  reactable and native formula/contracts tests pass. Clean isolated/production
  updater/application gates pass; active and rollback identities are in receipt.
- These are individual measured stat changes, NOT an upper error bound and NOT
  evidence that general helper/additive paths or artifact search are accepted.
- Flins cloud remains wholly frozen:25.262% /25.842% of the two baseline traces.
  This pilot does not claim cloud progress. No synthetic expected cloud result
  has been substituted for an engine observation.

## Remaining evidence and next bounded steps

1. **GP-3: contributor cloud.** The user already accepted preservation of each
   observed contributor, candidate-time stable sorting/weights from upstream,
   and expectation over at most16 independent crit states for4 participants.
   Sort inside each state, not after averaging individual damage. Last-trigger
   ownership is not the owner of all stats; the dummy final crit must not apply
   crit twice. Observed contributors/schedule are a frozen topology boundary.
   Only a new conceptual problem requires another discussion.
2. Fresh-engine changed-stat checks, both Chasca controls and updater gates
   remain mandatory; no per-candidate simulation brute force. Full supplied-team
   search follows dependency acceptance; Lauma2+2 legality still needs a decision.

## Reuse / cleanup

Editable source: `.codex_tmp/gob11-numeric-engine-store/engines/gcsim-v2.45.0-gob11-scalar-20260909`.
Despite its old directory name, it contains GP-2b and GP-1c-A/B/C. It is not a
separate Git repository. Do not edit the active production engine in place.

New captures/helper/isolated store: `.codex_tmp/gob11-flat-stat-20260912`,
`.codex_tmp/gob11_gp1c_check.py`, `.codex_tmp/gob11_flat_stat_export.py`,
`.codex_tmp/gob11-flat-stat-engine-store`. The GP-1c wrapper reuses the GP-2b
probe with explicit isolated paths and refuses production activation/fixture
overwrite. Tests/source are retained in the consolidated patch; two portable
formula samples (47/42 nodes) are permanent. Keep copied fixtures through GP-3;
then remove temporary reproductions, never active/pinned rollback engines.

B captures/build logs: `.codex_tmp/gob11-helper-return-20260912`; isolated
store: `.codex_tmp/gob11-helper-engine-store`; wrappers: `gob11_helper_check.py`
and `gob11_helper_export.py` under `.codex_tmp`. The shared probe's HELPER flag
includes all B source/runtime/compiler files when mechanically regenerating
the patch. One81-node fresh-engine sample and synthetic/full-tree regressions
are permanent. Historical B engine: `gcsim-v2.45.0-gob11-helper2-20260912`.
Both isolated/production updater gates and per-hit evaluator parity pass.
The earlier helper build without the field-position refinement is superseded,
not a production input. All temporary helper stores are cleanup candidates
after GP-3 acceptance; retain current active/pinned rollback engines.

C captures/build logs: `.codex_tmp/gob11-additive-20260913`; isolated store:
`.codex_tmp/gob11-additive-engine-store`; wrappers: `gob11_additive_check.py`
and `gob11_additive_export.py` under `.codex_tmp`. Shared probe flags GP1C/HELPER/
ADDITIVE include the complete source set in mechanical patch regeneration.
The prototype without original-vs-terminal validation is superseded; do not
rerun that diagnosis. Current engine:`gcsim-v2.45.0-gob11-additive2-20260913`;
rollback is B's helper2 engine. See the receipt for patch/artifact hashes.
Keep temporary copies only through GP-3 acceptance; then remove those copies,
not active/pinned rollback engines. No user data has been deleted or changed.

The old pause diff and failed isolated builds are superseded cleanup candidates.
The prototype's alias and template-marker errors are corrected; no unresolved
three-attempt blocker exists. Do not restart their diagnosis.
